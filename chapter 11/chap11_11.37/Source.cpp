//Exercise 11.37: What are the advantages of an unordered container as
//compared to the ordered version of that container ? What are the advantages
//of the ordered version ? the unordered container is useful when we don't need to have maintain keys in order and it average it gives better performance than an ordered container but there is too much upkeep to maintain it.
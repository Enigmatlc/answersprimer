//Exercise 11.34: What would happen if we used the subscript operator
//instead of find in the transform function ?
//it would return the item stored at s instead of returning an iterator to the item and if the item is not in the map it would insert it.
//Exercise 13.16: What if the parameter in f were const numbered& ?
//Does that change the output ? If so, why ? What output gets generated ? it wouldn't change anything since the function is not doing anything to the objects that would change them.
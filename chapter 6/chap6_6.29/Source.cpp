//Exercise 6.29: When you use an initializer_list in a range for
//would you ever use a reference as the loop control variable ? If so, why ? If
//not, why not?

//it depends of the datatype of the list. If its an int then i wouldnt use a reference, but if its a string i would use a reference.
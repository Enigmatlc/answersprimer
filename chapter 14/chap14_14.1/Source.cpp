//Exercise 14.1: In what ways does an overloaded operator differ from a
//built - in operator ? In what ways are overloaded operators the same as the
//built - in operators ? operators are overload to allow them to be used with class types and they should follow the same logic as the build in operators since it can make the class hard to understand if you change how the operators behave.
Exercise 7.25: Can Screen safely rely on the default versions of copyand
assignment ? If so, why ? If not, why not?

yes, since we provided values for the data memebers the same values will be used to initialize those members and the string class in always default initialize to hold an empty string.
//Exercise 7.20: When are friends useful ? Discuss the pros and cons of using
//friends.

//When members are encapsulated insided the private specifier non-member functionis no longer have access to those members the friend keyword is used to allow those functjions access to 
//the private members. One of he const of the friend keyword is that it make things more complicated depending on what compiler is being used to write the class since some times a separate 
//declaration has to be written outiside the class also.
//Exercise 7.44: Is the following declaration legal ? If not, why not?
//vector<NoDefault> vec(10);

//no since we need a default constructor for the vector to know what object type it will be storing.